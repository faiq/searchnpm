var lab = require('lab') 
  , describe = lab.describe
  , it = lab.it 
  , before = lab.before
  , expect = lab.expect;

describe('The constuctor', function () {
  it('should know what elasticsearch to connect to', function(done){ 

  }); 
  it('should have a default elasticsearch to connect to', function (done){ 

  });
});  
